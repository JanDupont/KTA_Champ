{
	"name": "kta_champ",
	"version": "1.0.0",
	"description": "",
	"main": "dist/server.js",
	"type": "module",
	"scripts": {
		"build": "npx tsc",
		"start:server": "node dist/server.js",
		"dev:server": "nodemon --watch src --watch server.ts --exec \"npm run build && node dist/server.js\"",
		"dev:frontend": "npm --prefix frontend run dev",
		"dev": "concurrently \"npm:dev:server\" \"npm:dev:frontend\"",
		"build:start": "npm run build && npm run start:server",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@types/cors": "^2.8.17",
		"@types/express": "^4.17.21",
		"concurrently": "^8.2.2",
		"nodemon": "^3.1.0",
		"ts-node": "^10.9.2",
		"typescript": "^5.4.5"
	},
	"dependencies": {
		"@types/cheerio": "^0.22.35",
		"axios": "^1.6.8",
		"cheerio": "^1.0.0-rc.12",
		"cors": "^2.8.5",
		"express": "^4.19.2"
	}
}
